{
  "name": "@starfolio/bitbucket-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Bitbucket REST client: fetches, paginates, and normalizes a user's repositories.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@starfolio/types": "workspace:*",
    "@starfolio/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2",
    "vitest": "^3.0.4"
  }
}
