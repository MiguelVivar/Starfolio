{
  "name": "@starfolio/exporters",
  "version": "0.1.0",
  "private": true,
  "description": "Format writers (CSV, JSON, Markdown, Excel) that turn a Repository[] into a downloadable file. Provider-agnostic: only depends on @starfolio/types.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@starfolio/types": "workspace:*",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.4"
  }
}
