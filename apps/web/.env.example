# Copy to .env.local and fill in a real token, then restart `pnpm dev`.
# Create one at https://github.com/settings/tokens/new?description=Starfolio&scopes=public_repo
# (classic token, "public_repo" read scope is enough). Never commit .env.local.
GITHUB_TOKEN=
